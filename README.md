# Minimal configuration for my linux development environment.

This configuration doesn't target to be versatile, but rather to be a simple and minimal configuration for my linux development environment.

It needs to install packages and tools manually, but it is easy to set up and maintain betweeen different machines and distros.

It is extensible and can be used as a base for other configurations.

Supported tools:

- zsh
- kitty
- git
